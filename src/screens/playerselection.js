import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const PlayerSelection = () => {
  return (
    <View>
      <Text>PlayerSelection</Text>
    </View>
  );
};

export default PlayerSelection;

const styles = StyleSheet.create({});
